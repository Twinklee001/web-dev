spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/farmtrack
    username: postgres
    password: 123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

app:
  jwt:
    secret: this-is-a-very-long-demo-secret-key-for-agri-track-2026
    expiration-ms: 86400000